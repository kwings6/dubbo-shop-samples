server:
  port: 10000

dubbo:
  application:
    qos-enable: false
    check-serializable: false
  registry:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181
  protocol:
    port: 20883
    name: dubbo